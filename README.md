# Resume Screening App

A machine learning-based Streamlit app to classify resumes into categories.

## Features
- Resume text cleaning
- TF-IDF vectorization
- ML model prediction
- Streamlit UI

## Setup

```bash
git clone <repo-link>
cd Resume-Screening-App
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt